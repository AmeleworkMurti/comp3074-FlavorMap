<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.flavormap">

    <application
        android:allowBackup="true"
        android:label="FlavorMap"
        android:theme="@style/Theme.FlavorMap">
        <activity
            android:name=".ui.RestaurantListActivity"
            android:exported="false" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="false" />

        <activity android:name=".ui.RestaurantDetailsActivity"></activity>
        <activity android:name=".ui.AddRestaurantActivity"/>

        <activity android:name=".ui.AboutUsActivity" />

        <activity
            android:name=".ui.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>